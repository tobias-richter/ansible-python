# global index url for pip
# python_global_index_url: "https://pypi.python.org/simple"

# Use this configuration option when your pypi mirror has no valid https certificate
# python_global_trusted_host: pypi.python.org

# Python os packages to install
# see vars/ for defined packages

# The pip package and version to install
python_pip_package_pip: pip>=20.3.1

# The setups tools package and version to install
python_pip_package_setuptools: setuptools>=47

# List of pip packages to install
python_pip_packages: []

# Controls if setuptool will be updated
python_update_setuptools: yes

# Controls if pip will be updated
python_update_pip: yes